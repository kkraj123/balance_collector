name: collector_app
description: "A new Flutter project."

publish_to: "none"

version: 1.5.32

environment:
  # sdk: ">=2.17.0 <4.0.0"
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  path: ^1.8.3
  # sqflite: ^2.3.3
  sqflite: ^2.4.0
  mobile_scanner: ^5.0.0
  # qr_code_scanner: ^1.0.1
  # image_picker: ^1.0.1
  image_picker: ^1.2.2
  # scan: ^1.6.0
  mime: ^1.0.4
  cupertino_icons: ^1.0.6
  flutter_svg: ^2.0.9
  dio: ^5.7.0
  flutter_bloc: ^8.1.6
  # mime: ^2.0.0
  equatable: ^2.0.7
  shared_preferences: ^2.2.3
  # permission_handler: ^10.2.0
  permission_handler: ^11.3.1
  # url_launcher: ^6.3.1
  url_launcher: ^6.3.0+1
  # intl: ^0.18.1
  intl: ^0.20.2
  # geolocator: ^11.0.0
  geolocator: ^13.0.1
  uuid: ^4.5.1
  # device_preview: ^1.1.0
  sunmi_printer_plus: ^2.1.2
  number_to_words_nepali: ^1.3.0
  # connectivity_plus: ^5.0.2
  connectivity_plus: ^6.0.5
  senraise_printer: ^0.0.6
  device_info_plus: ^10.1.0
  path_provider: ^2.1.5
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  
dependency_overrides:
  flutter_plugin_android_lifecycle: ^2.0.30
  image_picker_android: 0.8.12+17
  shared_preferences_android: ^2.3.0
  url_launcher_android: ^6.3.10        # add this
  geolocator_android: ^5.0.0           # add this (was flagged too)
  permission_handler_android: ^13.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/icons/balance.png
    - assets/icons/Splash.png
    - assets/icons/group.png
    - assets/icons/
